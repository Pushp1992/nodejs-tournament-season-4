// All controller logic for CRUD operation will stay here

const MeetupModel = require('../model/meetup-model');

// Create and Save a new Member Profile
exports.create = (req, res) => {
    
    // validate request body

    // Creaate Member Profile

    // Save Member Profile in Database
 
};


// Retrieve and return all Member Profile from the database.
exports.findAll = (req, res) => {
   // TODO from below
};

// Find a single Member Profile with a memberProfileId
exports.findOne = (req, res) => {
    // TODO from below
};

// Update a MemberProfile identified by the memberProfileId in the request
exports.update = (req, res) => {
    // TODO from below
};

// Delete a MemberProfile with the specified memberProfileId in the request
exports.delete = (req, res) => {
    // TODO from below
};