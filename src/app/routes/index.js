// all routes HTTP method (GET/PUT/POST/DELETE) will be present here

module.exports = (app) => {
    // import your existing Meetup controller here, which contains method for all CRUD operations

    // APi endpoint to create a new Member profile

    // API endpoint to get all Member profile

    // API endpoint to get single Member profile

    // API endpoint to update Member Profile with profileId

    // API endpoint to delete Member Profile with profileId

};